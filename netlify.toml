[build]
  command = "npm install -g pnpm && pnpm install && pnpm --filter @workspace/chess-bishop run build"
  publish = "artifacts/chess-bishop/dist/public"

[build.environment]
  NODE_VERSION = "20"
  BASE_PATH = "/"
